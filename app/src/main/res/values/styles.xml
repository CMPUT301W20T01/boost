<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colourPrimary</item>
        <item name="colorPrimaryDark">@color/colourPrimaryDark</item>
        <item name="colorAccent">@color/colourGray</item>
        <item name="buttonStyle">@style/BoostButtonStyle</item>
        <item name="colorControlHighlight">@color/colourGray</item>
    </style>

    <style name="BoostButtonStyle" parent="@style/Widget.AppCompat.Button">
        <item name="fontFamily">@font/lato_black</item>
    </style>

</resources>
